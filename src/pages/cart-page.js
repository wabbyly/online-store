import React from 'react';
import Cart from '../features/cart';

export default function CartPage(props){
    return(
        <div>
            <h2>cart page</h2>
            <Cart />
        </div>
    );
}